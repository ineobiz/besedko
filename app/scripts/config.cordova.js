'use strict';

angular.module('webApp').constant('CONFIG', {
    remote : 'http://besedko.os-jela-janezica.si',
    povej : {
        buttons : {
            favorite   : false,
            volume     : false,
            keyboard   : true,
            fullscreen : false
        }
    }
});