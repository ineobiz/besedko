'use strict';

angular.module('webApp').constant('CONFIG', {
    povej : {
        buttons : {
            favorite   : false,
            volume     : false,
            keyboard   : true,
            fullscreen : false
        }
    }
});